import { AppForbiddenNameDirective } from './app-forbidden-name.directive';

describe('AppForbiddenNameDirective', () => {
  it('should create an instance', () => {
    const directive = new AppForbiddenNameDirective();
    expect(directive).toBeTruthy();
  });
});
